#include <iostream>

struct Node
{
    int data;
    Node *next;
}


Node initNode
{
    Node *ptr;
    ptr = new Node;
    ptr->data = 10;
    ptr->next = nullptr;
}

int main(int argc, int argv )
{
        
    
    struct Node *ptr;
    ptr = new Node;
    p->next
        
